# --- Required secrets ---
DB_PASSWORD=your_db_password

# --- Direct connection (recommended for FastAPI service) ---
DATABASE_URL_DIRECT=postgresql://postgres:${DB_PASSWORD}@db.<PROJECT_REF>.supabase.co:5432/postgres?sslmode=require

# --- Pooled (transaction mode) for serverless / spikes ---
# Note the different HOST and PORT 6543, and pgbouncer flag.
DATABASE_URL_POOLED_TX=postgresql://postgres.<PROJECT_REF>:${DB_PASSWORD}@<CLOUD>-0-<REGION>.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true

# Toggle which one the app uses: direct | pooled_tx
DB_CONNECTION_MODE=direct
